# pclib-db-sample-v2

Run this module through the root `run-sample.sh`, or start the databases with Docker Compose and then run:

```bash
mvn -f pclib-db-sample-v2/pom.xml spring-boot:run
```

The database settings are in `src/main/resources/application.yml`.
